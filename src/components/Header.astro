<header
    class='sticky top-0 z-50 bg-background/85 text-foreground backdrop-blur-xl'
>
    <div class='mx-auto max-w-6xl px-4 sm:px-6 lg:px-8'>
        <nav class='flex h-16 items-center justify-between'>
            <div class='shrink-0'>
                <a href='/' title='Ir al inicio' class='text-lg font-bold'>
                    Nextlevel
                </a>
            </div>

            <a
                id='HeaderCTA'
                title='Envianos un mensaje por WhatsApp'
                href='https://wa.me/5491151475056'
                title='Comenzar la asesoría'
                class='ml-10 hidden items-center justify-center rounded-lg border border-transparent bg-accent-600 px-4 py-2 text-base font-semibold text-accent-foreground transition-all duration-200 hover:bg-accent-500 focus:bg-accent-700 sm:inline-flex'
            >
                Empezar ahora
            </a>
        </nav>
    </div>
</header>

<style>
    #HeaderCTA {
        animation: show-button ease both 1ms;
        animation-timeline: scroll(root block);
        animation-range: 500px 600px;
    }

    @keyframes show-button {
        from {
            opacity: 0;
        }

        to {
            opacity: 1;
        }
    }
</style>
